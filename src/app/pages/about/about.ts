import {Component} from '@angular/core';
import {Header} from '../../components/header';

@Component({
  selector: 'app-about',
  imports: [Header],
  templateUrl: './about.html',
})
export class About {}
