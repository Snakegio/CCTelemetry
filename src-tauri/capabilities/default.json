{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Read Claude Code session logs and persist daily history via custom commands",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "updater:default",
    "notification:default",
    "autostart:allow-enable",
    "autostart:allow-disable",
    "autostart:allow-is-enabled"
  ]
}
